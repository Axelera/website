import { defineConfig } from '@junobuild/config';

/** @type {import('@junobuild/config').JunoConfig} */
export default defineConfig({
  satellite: {
    id: 'sg4ml-jiaaa-aaaal-ascma-cai',
    source: 'public',
  },
  orbiter: {
    id: 'ugrbl-7aaaa-aaaas-amlqq-cai',
  },
});
